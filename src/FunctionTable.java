
public class FunctionTable {
    public static double[] functionTable(double min, int numEntries, souble delta) {
        double[] table = new double(numEntries);
        double x = minX;
        for(int i = 0; < numEntries; i++, x = x + delta) {
            
        }
    }
}
